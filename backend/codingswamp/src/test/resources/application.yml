spring:
  sql:
    init:
      mode: always

  datasource:
    url: jdbc:h2:mem:~/test;MODE=MYSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
